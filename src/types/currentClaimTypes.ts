export type CurrentClaimTypes = {
  createdAt?: Date;
  description?: string;
  status?: Record<string, any>;
title?: string;
  type?: Record<string, any>;
  updateAt?: Date;
  user?: string;
  _id?: string;

  noData?: string;
};
