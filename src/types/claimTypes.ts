export type ClaimTypes = {
  createdAt?: string;
  description?: string;
  status?: Record<string, any>;
  title?: string;
  type?: Record<string, any>;
  updateAt?: Date;
  user?: string;
  _id?: string;

  noData?: string;
};
